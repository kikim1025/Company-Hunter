import React from 'react';
import Header from './Header';
import MainContainer from '../container/MainContainer';

const Page = () => (
    <div id='page'>
        <Header />
        <MainContainer />
    </div>
);

export default Page;